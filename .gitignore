# Rust build output
/target

# Debian build artifacts (generated by `dpkg-buildpackage` / `debhelper`)
debian/.debhelper/
debian/cargo_home/
debian/cosmic-nightlight/
debian/debhelper-build-stamp
debian/files
debian/*.substvars
debian/*.debhelper
debian/*.log

# Flatpak build artifacts. cargo-sources.json is generated from Cargo.lock
# (see flatpak/README.md); cosmic-flatpak carries its own committed copy.
flatpak/cargo-sources.json
flatpak/*.local.json
flatpak/.flatpak-builder/
flatpak/build/
flatpak/venv/
flatpak/flatpak-cargo-generator.py

# Local Claude Code settings
.claude/settings.local.json
